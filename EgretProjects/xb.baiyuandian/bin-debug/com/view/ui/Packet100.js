/**
 * 红包100
 * @author
 *
 */
var Packet100 = (function (_super) {
    __extends(Packet100, _super);
    function Packet100() {
        _super.call(this, "Packet100", 100, RES.getRes("packet100"), RES.getRes("packetbg"));
    }
    var d = __define,c=Packet100,p=c.prototype;
    return Packet100;
})(BasePacket);
egret.registerClass(Packet100,'Packet100');
